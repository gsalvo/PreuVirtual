<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_centerHorizontal="true"
    android:layout_centerVertical="true"
    android:gravity="center"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:weightSum="1"
    tools:context=".MainActivity">>

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingBottom="10dp"
        android:src="@drawable/titulo" />

    <Button
        android:id="@+id/btnTimeAttack"
        style="@style/CustomButton"
        android:drawableLeft="@drawable/ic_alarm_white_24dp"
        android:onClick="goTimeAttack"
        android:text="Cuenta regresiva" />

    <Button
        android:id="@+id/btnEndless"
        style="@style/CustomButton"
        android:backgroundTint="@color/abc_primary_text_material_light"
        android:drawableLeft="@drawable/ic_endless_24dp"
        android:onClick="goEndless"
        android:text="Puntaje Nacional" />

    <Button
        android:id="@+id/btnGoals"
        style="@style/CustomButton"
        android:backgroundTint="@color/abc_primary_text_material_light"
        android:drawableLeft="@drawable/ic_goal_24dp"
        android:enabled="false"
        android:text="Objetivos"
        android:visibility="gone"/>

</LinearLayout>
